# dsh-pet bundle patch: inserts its plugin row into the web plugin roster.
# The row references the installed package name, as required by the official
# bundle format. It mounts the Host service and the browser settings card;
# the packaged desktop runtime is managed by the Host plugin lifecycle.
- insert:
    - id: pet
      name: '@linxin666/dsh-pet'
